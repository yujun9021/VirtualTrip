import React from 'react';
import UploadForm from '../components/UploadForm';

const HomePage = () => {
    return (
        <div>
            <h1>Home Page</h1>
            <UploadForm />
        </div>
    );
};

export default HomePage;
